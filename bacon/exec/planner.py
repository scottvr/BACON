def planner(state):
    # For now, we'll hardcode a tool call to search for "who is Jules".
    # Later, this will be generated by an LLM.
    tool_call = {
        "tool": "search",
        "params": {"query": "who is Jules"}
    }
    return {"messages": [tool_call]}